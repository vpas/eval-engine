# vitest/@vitejs-plugin-react peer on a newer @types/node than the app pins (20.16); the conflict is
# optional type stubs only. legacy-peer-deps keeps `npm ci`/`npm install` (incl. the Docker build) green.
legacy-peer-deps=true
