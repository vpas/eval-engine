.venv/
__pycache__/
*.pyc
.data/
logs/
*.egg-info/
build/
dist/
# local MCP server config: machine-specific venv path + dev-only RO creds (see docs/DEVELOPMENT.md)
.mcp.json
.env
get_helm.sh
.claude/
# grafana dashboards-as-code: vendored grafonnet + rendered output + jsonnet/go tooling caches are
# regenerated by `make render` (jsonnetfile.lock.json IS committed for reproducible renders). build/
# is already ignored above.
deploy/grafana/vendor/
deploy/grafana/.go/
deploy/grafana/.config/
deploy/grafana/.gocache/
# stray jsonnet source tarball (tool is installed to /usr/local + $GOPATH/bin)
/jsonnet-*.tar.gz
